---
/**
 * Image Tools Pillar Page - Topical Authority Hub
 *
 * SEO Targets: "free image tools online", "image editor free"
 * Purpose: Establish expertise for all image-related tools
 * Strategy: Comprehensive category page with E-E-A-T signals
 */
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/ui/Navbar.astro';
import Footer from '../components/ui/Footer.astro';
import ToolCard from '../components/ui/ToolCard.astro';
import { getToolsByCategory } from '../lib/tools';

// Get all media tools and filter for image-specific ones
const allMediaTools = getToolsByCategory('media');
const imageTools = allMediaTools.filter(tool =>
  ['image-compress', 'file-converter', 'background-remover', 'exif-editor'].includes(tool.id)
);

// SEO metadata
const title = 'Free Image Tools Online - Compress, Convert & Edit Images | New Life Solutions';
const description = 'Complete suite of free image tools running in your browser. Compress images 90%, convert HEIC to JPG, remove backgrounds with AI, and strip EXIF metadata. No upload required.';
const baseUrl = 'https://www.newlifesolutions.dev';
const currentDate = new Date().toISOString().split('T')[0];
const lastUpdated = new Date().toLocaleDateString('en-US', { month: 'long', year: 'numeric' });

// FAQ data
const faqs = [
  {
    question: 'Are these image tools completely free?',
    answer: 'Yes, all image tools are 100% free with no watermarks, sign-up requirements, or hidden fees. Process unlimited images with no daily quotas or restrictions.'
  },
  {
    question: 'Are my photos uploaded to a server?',
    answer: 'No. All image processing happens entirely in your browser using JavaScript and Canvas API. Your photos never leave your device, ensuring complete privacy and security.'
  },
  {
    question: 'How much can images be compressed?',
    answer: 'Typically 60-90% size reduction while maintaining visual quality. Results vary by image type: photos compress more than graphics, and lossy formats (JPEG, WebP) compress more than lossless (PNG).'
  },
  {
    question: 'Can I convert HEIC files from my iPhone?',
    answer: 'Yes! The Image Converter tool converts HEIC (iPhone photo format) to JPG, PNG, or WebP. Batch conversion supported with ZIP download. Windows users can now open iPhone photos easily.'
  },
  {
    question: 'What is EXIF metadata and why remove it?',
    answer: 'EXIF metadata includes GPS location, camera model, date, and settings embedded in photos. Removing it protects your privacy by preventing others from seeing your location or when/where photos were taken.'
  },
  {
    question: 'Does AI background removal work on all images?',
    answer: 'Best results with clear subjects and distinct backgrounds. Works on people, products, animals, and objects. May struggle with complex scenes, hair details, or transparent objects.'
  },
  {
    question: 'What image formats are supported?',
    answer: 'Input: PNG, JPEG, WebP, HEIC, BMP, GIF. Output: PNG (transparency), JPEG (photos), WebP (modern). Format support varies by tool—check individual tool pages for specifics.'
  }
];

// Common workflows
const commonWorkflows = [
  {
    title: 'Prepare Product Photos for E-commerce',
    steps: [
      'Remove background with AI Background Remover',
      'Convert to PNG for transparency',
      'Compress image to reduce file size',
      'Strip EXIF metadata for privacy',
      'Upload to Amazon, Etsy, or Shopify'
    ]
  },
  {
    title: 'Convert iPhone Photos for Windows',
    steps: [
      'Open Image Converter tool',
      'Drag HEIC files from iPhone',
      'Select JPG output format',
      'Set quality to 90% or higher',
      'Download converted images as ZIP'
    ]
  },
  {
    title: 'Remove Location Data Before Sharing',
    steps: [
      'Open EXIF Metadata Editor',
      'Upload your photos',
      'Click "Strip All Metadata"',
      'Preview remaining data',
      'Download cleaned images'
    ]
  }
];

// Schema markup
const pillarSchema = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  '@id': `${baseUrl}/image-tools#collection`,
  name: 'Free Image Tools Online - Complete Suite 2025',
  description: description,
  url: `${baseUrl}/image-tools`,
  datePublished: '2024-12-01',
  dateModified: currentDate,
  inLanguage: 'en-US',
  isPartOf: {
    '@type': 'WebSite',
    name: 'New Life Solutions',
    url: baseUrl
  },
  mainEntity: {
    '@type': 'ItemList',
    name: 'Image Tools',
    numberOfItems: imageTools.length,
    itemListElement: imageTools.map((tool, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: tool.name,
      description: tool.description,
      url: `${baseUrl}${tool.href}`
    }))
  }
};

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.answer
    }
  }))
};

const howToSchemas = commonWorkflows.map((workflow) => ({
  '@context': 'https://schema.org',
  '@type': 'HowTo',
  name: workflow.title,
  step: workflow.steps.map((stepText, index) => ({
    '@type': 'HowToStep',
    position: index + 1,
    text: stepText
  }))
}));

const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: baseUrl },
    { '@type': 'ListItem', position: 2, name: 'Image Tools', item: `${baseUrl}/image-tools` }
  ]
};
---

<Layout title={title} description={description} image="/thumbnails/image-compress.svg">
  <script type="application/ld+json" set:html={JSON.stringify(pillarSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  {howToSchemas.map((schema) => (
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
  ))}
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />

  <Navbar />

  <main class="pt-20 pb-16 px-6 min-h-screen">
    <div class="max-w-6xl mx-auto">

      <header class="text-center mb-16">
        <a href="/hub" class="back-link">
          <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
          Back to Tool Hub
        </a>

        <div class="text-3xl mb-6 opacity-60">🖼️</div>

        <h1 class="category-title">
          Free Image Tools Online
        </h1>

        <p class="category-intro">
          Professional image editing suite that runs entirely in your browser. Compress images up to 90% smaller
          without losing quality, convert HEIC iPhone photos to JPG/PNG, remove backgrounds with AI,
          strip GPS location and EXIF metadata for privacy. Perfect for photographers, e-commerce sellers,
          marketers, and anyone who values privacy. All processing happens locally—your images never
          leave your device. No signup, no uploads to servers, no watermarks. Free forever.
        </p>

        <div class="trust-signals">
          <span class="trust-signal">
            <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
            </svg>
            AI-Powered Processing
          </span>
          <span class="trust-signal">
            <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
            </svg>
            No Watermarks
          </span>
          <span class="trust-signal">
            <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
            </svg>
            Batch Processing
          </span>
          <span class="trust-signal">
            <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
            </svg>
            100% Private
          </span>
        </div>

        <p class="updated-date">
          <time datetime={currentDate}>Last updated: {lastUpdated}</time>
        </p>
      </header>

      <section class="how-section">
        <h2 class="section-heading">
          <span class="heading-line"></span>
          How it works
        </h2>

        <div class="how-grid">
          <div class="how-card">
            <div class="how-step">01</div>
            <h3 class="how-title">Choose an image tool</h3>
            <p class="how-text">Pick compression, conversion, background removal, or metadata cleanup.</p>
          </div>
          <div class="how-card">
            <div class="how-step">02</div>
            <h3 class="how-title">Upload locally</h3>
            <p class="how-text">Add photos from your device. Processing stays in your browser.</p>
          </div>
          <div class="how-card">
            <div class="how-step">03</div>
            <h3 class="how-title">Download results</h3>
            <p class="how-text">Save optimized images immediately with no watermarks.</p>
          </div>
        </div>
      </section>

      <section class="tools-grid tool-grid" aria-label="Image Tools Collection">
        {imageTools.map((tool) => (
          <ToolCard tool={tool} />
        ))}
      </section>

      <section class="workflows-section">
        <h2 class="section-heading">
          <span class="heading-line"></span>
          Common Workflows
        </h2>

        <div class="workflows-grid">
          {commonWorkflows.map((workflow) => (
            <article class="workflow-card">
              <h3 class="workflow-title">{workflow.title}</h3>
              <ol class="workflow-steps">
                {workflow.steps.map((step, index) => (
                  <li class="workflow-step">
                    <span class="step-number">{String(index + 1).padStart(2, '0')}</span>
                    <span class="step-text">{step}</span>
                  </li>
                ))}
              </ol>
            </article>
          ))}
        </div>
      </section>

      <section class="why-section">
        <h2 class="section-heading">
          <span class="heading-line"></span>
          Why Browser-Based Image Tools?
        </h2>

        <div class="why-grid">
          <div class="why-card">
            <div class="why-icon">🔐</div>
            <h3 class="why-title">Total Privacy</h3>
            <p class="why-text">
              Your photos never leave your device. No uploads to servers means no data collection, no privacy risks,
              no unauthorized access. Perfect for sensitive photos, product prototypes, or personal images.
            </p>
          </div>

          <div class="why-card">
            <div class="why-icon">🚀</div>
            <h3 class="why-title">Instant Processing</h3>
            <p class="why-text">
              No upload/download delays. Process images instantly using modern Canvas API and WebGL. Compress 50 photos
              in seconds, not minutes. Works on mobile devices too.
            </p>
          </div>

          <div class="why-card">
            <div class="why-icon">🎨</div>
            <h3 class="why-title">Professional Quality</h3>
            <p class="why-text">
              AI-powered tools produce professional results. ESRGAN upscaling, background removal with edge detection,
              smart compression algorithms. No quality compromise.
            </p>
          </div>

          <div class="why-card">
            <div class="why-icon">💰</div>
            <h3 class="why-title">Always Free</h3>
            <p class="why-text">
              No subscription fees, no credits system, no watermarks on output. Process unlimited images forever.
              Perfect for small businesses, freelancers, and hobbyists.
            </p>
          </div>
        </div>
      </section>

      <section class="faq-section">
        <h2 class="section-heading">
          <span class="heading-line"></span>
          Frequently Asked Questions
        </h2>

        <div class="faq-list">
          {faqs.map((faq) => (
            <details class="faq-item">
              <summary class="faq-question">{faq.question}</summary>
              <div class="faq-answer">
                <p>{faq.answer}</p>
              </div>
            </details>
          ))}
        </div>
      </section>

      <section class="related-section">
        <h2 class="section-heading">
          <span class="heading-line"></span>
          Explore More Tools
        </h2>

        <div class="related-grid">
          <a href="/pdf-tools" class="related-card">
            <div class="related-icon">📄</div>
            <h3 class="related-title">PDF Tools</h3>
            <p class="related-text">Merge, compress, split, and edit PDFs with professional browser-based tools</p>
          </a>

          <a href="/video-tools" class="related-card">
            <div class="related-icon">📹</div>
            <h3 class="related-title">Video Tools</h3>
            <p class="related-text">Compress, trim, and convert videos. Extract audio, create GIFs, and generate subtitles</p>
          </a>

          <a href="/audio-tools" class="related-card">
            <div class="related-icon">🎵</div>
            <h3 class="related-title">Audio Tools</h3>
            <p class="related-text">Edit waveforms, remove vocals, transcribe with AI, and create audiograms</p>
          </a>
        </div>
      </section>

    </div>
  </main>

  <Footer />
</Layout>

<style>
  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--text-muted);
    text-decoration: none;
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 2rem;
    transition: color 0.2s ease;
  }

  .back-link:hover {
    color: var(--text);
  }

  .category-title {
    font-size: 2rem;
    font-weight: 400;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
    color: var(--text);
  }

  .category-intro {
    font-size: 0.9375rem;
    line-height: 1.8;
    color: var(--text-dim);
    max-width: 48rem;
    margin: 0 auto 2rem;
  }

  .trust-signals {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 1rem;
  }

  .trust-signal {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-dim);
  }

  .check-icon {
    width: 1rem;
    height: 1rem;
    color: #22c55e;
  }

  .updated-date {
    font-size: 0.6875rem;
    color: var(--text-muted);
    letter-spacing: 0.05em;
  }

  .updated-date time {
    color: var(--text-dim);
  }

  .tools-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1rem;
    margin-bottom: 4rem;
  }

  .section-heading {
    font-size: 0.875rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--text);
    margin-bottom: 2rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .heading-line {
    width: 2rem;
    height: 1px;
    background: var(--text-muted);
  }

  .workflows-section {
    margin-bottom: 4rem;
    padding-top: 3rem;
    border-top: 1px solid var(--border);
  }

  .how-section {
    margin-bottom: 4rem;
    padding-top: 3rem;
    border-top: 1px solid var(--border);
  }

  .how-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.5rem;
  }

  .how-card {
    border: 1px solid var(--border);
    padding: 1.5rem;
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.02) 0%,
      rgba(255, 255, 255, 0.05) 100%
    );
  }

  .how-step {
    font-size: 0.625rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 0.5rem;
  }

  .how-title {
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .how-text {
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--text-dim);
    margin: 0;
  }

  .workflows-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .workflow-card {
    border: 1px solid var(--border);
    padding: 1.5rem;
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.02) 0%,
      rgba(255, 255, 255, 0.05) 100%
    );
  }

  .workflow-title {
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text);
    margin-bottom: 1rem;
  }

  .workflow-steps {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .workflow-step {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    font-size: 0.8125rem;
    color: var(--text-dim);
  }

  .step-number {
    font-size: 0.625rem;
    font-weight: 600;
    color: var(--text-muted);
    min-width: 1.5rem;
  }

  .step-text {
    flex: 1;
  }

  .why-section {
    margin-bottom: 4rem;
    padding-top: 3rem;
    border-top: 1px solid var(--border);
  }

  .why-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .why-card {
    text-align: center;
  }

  .why-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .why-title {
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text);
    margin-bottom: 0.75rem;
  }

  .why-text {
    font-size: 0.8125rem;
    line-height: 1.7;
    color: var(--text-dim);
  }

  .faq-section {
    margin-bottom: 4rem;
    padding-top: 3rem;
    border-top: 1px solid var(--border);
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    max-width: 48rem;
  }

  .faq-item {
    border: 1px solid var(--border);
    background: transparent;
  }

  .faq-question {
    padding: 1rem;
    font-size: 0.8125rem;
    color: var(--text);
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-question::after {
    content: '+';
    font-size: 1rem;
    color: var(--text-muted);
  }

  .faq-item[open] .faq-question::after {
    content: '−';
  }

  .faq-answer {
    padding: 0 1rem 1rem;
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--text-dim);
  }

  .related-section {
    padding-top: 3rem;
    border-top: 1px solid var(--border);
  }

  .related-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }

  .related-card {
    border: 1px solid var(--border);
    padding: 1.5rem;
    text-decoration: none;
    text-align: center;
    transition: all 0.3s ease;
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.02) 0%,
      rgba(255, 255, 255, 0.05) 100%
    );
  }

  .related-card:hover {
    border-color: var(--text-muted);
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.05) 0%,
      rgba(255, 255, 255, 0.08) 100%
    );
  }

  .related-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .related-title {
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .related-text {
    font-size: 0.75rem;
    line-height: 1.6;
    color: var(--text-dim);
  }

  @media (max-width: 640px) {
    .category-title {
      font-size: 1.5rem;
    }

    .tools-grid {
      grid-template-columns: 1fr;
    }

    .workflows-grid,
    .why-grid,
    .related-grid {
      grid-template-columns: 1fr;
    }

    .trust-signals {
      flex-direction: column;
      gap: 0.75rem;
    }
  }
</style>
