---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/ui/Navbar.astro';
import Footer from '../../components/ui/Footer.astro';
import ToolCard from '../../components/ui/ToolCard.astro';
import { getToolsByCategory } from '../../lib/tools';

const tools = getToolsByCategory('document');

// SEO metadata
const title = 'Free PDF Tools Online | Merge, Compress, Split, Convert PDFs';
const description = 'Complete PDF toolkit - merge, compress, split, redact, convert PDF to Word/JPG, fill forms, organize pages. 100% browser-based, no uploads required.';
---

<Layout title={title} description={description} canonicalUrl="https://www.newlifesolutions.dev/hub/pdf-tools">
  <Navbar />
  <main class="pt-16">
    <!-- Hero Section -->
    <section class="py-20 px-6">
      <div class="max-w-4xl mx-auto text-center">
        <div class="text-[0.625rem] uppercase tracking-[0.3em] text-[var(--text-muted)] mb-4">PDF Tools Hub</div>
        <h1 class="text-3xl sm:text-4xl font-semibold mb-6">
          Complete PDF Toolkit
        </h1>
        <p class="text-[var(--text-dim)] text-base max-w-2xl mx-auto leading-relaxed mb-8">
          Everything you need to work with PDFs - merge multiple documents, compress large files, split pages,
          redact sensitive information, convert to Word or images, fill forms, and organize pages.
          All processing happens in your browser for maximum privacy.
        </p>
        <div class="flex flex-wrap justify-center gap-4 text-[0.625rem] text-[var(--text-muted)] uppercase tracking-[0.15em]">
          <span>No File Uploads</span>
          <span>Bank-Level Security</span>
          <span>Unlimited Usage</span>
        </div>
      </div>
    </section>

    <!-- Tools Grid -->
    <section id="tools" class="py-20 px-6 border-t border-[var(--border)]">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-2xl sm:text-3xl font-semibold mb-4">
            {tools.length} PDF Tools Available
          </h2>
          <p class="text-[var(--text-dim)] text-sm max-w-2xl mx-auto leading-relaxed">
            Click any tool to start processing your PDFs immediately. All tools work offline and support large files up to 50MB.
          </p>
        </div>

        <div class="tool-grid">
          {tools.map(tool => (
            <ToolCard tool={tool} />
          ))}
        </div>
      </div>
    </section>

    <!-- Benefits Section -->
    <section id="benefits" class="py-20 px-6 border-t border-[var(--border)]">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-2xl sm:text-3xl font-semibold mb-6">Why Choose Our PDF Tools?</h2>
        </div>

        <div class="grid md:grid-cols-2 gap-8">
          <div class="glass-card p-8">
            <h3 class="text-lg font-semibold mb-4">Privacy First</h3>
            <p class="text-[var(--text-dim)] text-sm leading-relaxed">
              Your PDFs never leave your device. All processing happens locally in your browser using
              WebAssembly technology. No cloud servers, no data transmission, complete privacy.
            </p>
          </div>

          <div class="glass-card p-8">
            <h3 class="text-lg font-semibold mb-4">Professional Quality</h3>
            <p class="text-[var(--text-dim)] text-sm leading-relaxed">
              Built with industry-standard PDF libraries for accurate, reliable results.
              Preserve formatting, maintain quality, and handle complex PDF structures with ease.
            </p>
          </div>

          <div class="glass-card p-8">
            <h3 class="text-lg font-semibold mb-4">Lightning Fast</h3>
            <p class="text-[var(--text-dim)] text-sm leading-relaxed">
              Process PDFs instantly with multi-threaded WebAssembly engines.
              No queue times, no waiting for uploads - just drag, drop, and download.
            </p>
          </div>

          <div class="glass-card p-8">
            <h3 class="text-lg font-semibold mb-4">Completely Free</h3>
            <p class="text-[var(--text-dim)] text-sm leading-relaxed">
              Unlimited usage with no hidden costs. No watermarks, no file limits,
              no registration required. Use as much as you want, whenever you need.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Voice-Optimized FAQ Section for Hub Page -->
    <section class="py-20 px-6 border-t border-[var(--border)]" aria-labelledby="faq-heading">
      <div class="max-w-4xl mx-auto">
        <h2 id="faq-heading" class="text-2xl sm:text-3xl font-semibold text-center mb-12">
          Frequently Asked Questions About PDF Tools
        </h2>

        <div class="space-y-6" itemscope itemtype="https://schema.org/FAQPage">
          <div class="faq-item" itemprop="mainEntity" itemscope itemtype="https://schema.org/Question">
            <h3 itemprop="name" class="text-lg font-semibold mb-3">What's the best free PDF merge tool online?</h3>
            <div itemprop="acceptedAnswer" itemscope itemtype="https://schema.org/Answer">
              <div itemprop="text" class="text-[var(--text-dim)]">
                <p>The best free PDF merge tool processes files in your browser without uploading them to a server. Our PDF Merge tool at New Life Solutions combines multiple PDFs instantly with drag-and-drop interface, unlimited files, no watermarks, and full privacy since nothing leaves your device. It's completely free and works offline after page load.</p>
              </div>
            </div>
          </div>

          <div class="faq-item" itemprop="mainEntity" itemscope itemtype="https://schema.org/Question">
            <h3 itemprop="name" class="text-lg font-semibold mb-3">How can I compress a PDF file size for free?</h3>
            <div itemprop="acceptedAnswer" itemscope itemtype="https://schema.org/Answer">
              <div itemprop="text" class="text-[var(--text-dim)]">
                <p>To compress a PDF for free, use a browser-based tool that uses intelligent compression algorithms. Upload your PDF to our PDF Compress tool, and it will reduce file size by up to 90% by removing unnecessary metadata, optimizing images, and flattening forms. All processing happens locally with no server uploads, keeping your document private and secure.</p>
              </div>
            </div>
          </div>

          <div class="faq-item" itemprop="mainEntity" itemscope itemtype="https://schema.org/Question">
            <h3 itemprop="name" class="text-lg font-semibold mb-3">Is there a truly free PDF editor that doesn't add watermarks?</h3>
            <div itemprop="acceptedAnswer" itemscope itemtype="https://schema.org/Answer">
              <div itemprop="text" class="text-[var(--text-dim)]">
                <p>Yes, New Life Solutions offers completely free PDF tools with no watermarks. Our form filler, redactor, and organizer tools handle PDF editing entirely in your browser without requiring registration or adding watermarks. The difference is client-side processing - we don't need to brand your files because we never see them.</p>
              </div>
            </div>
          </div>

          <div class="faq-item" itemprop="mainEntity" itemscope itemtype="https://schema.org/Question">
            <h3 itemprop="name" class="text-lg font-semibold mb-3">How do I convert PDF to Word for free without losing formatting?</h3>
            <div itemprop="acceptedAnswer" itemscope itemtype="https://schema.org/Answer">
              <div itemprop="text" class="text-[var(--text-dim)]">
                <p>Our PDF to Word converter preserves formatting by extracting text, images, and layout elements using pdf-lib. Upload your PDF, and the tool creates an editable Word document with maintained structure. The conversion happens entirely in your browser - no uploads, no formatting loss from server compression, and complete privacy for sensitive documents.</p>
              </div>
            </div>
          </div>

          <div class="faq-item" itemprop="mainEntity" itemscope itemtype="https://schema.org/Question">
            <h3 itemprop="name" class="text-lg font-semibold mb-3">Can I split PDF pages into separate files online?</h3>
            <div itemprop="acceptedAnswer" itemscope itemtype="https://schema.org/Answer">
              <div itemprop="text" class="text-[var(--text-dim)]">
                <p>Yes, our PDF Split tool extracts individual pages or ranges into separate files. You can select specific pages, extract them as individual PDFs or a ZIP archive, and download instantly. The entire process happens locally - your PDF never leaves your browser, making it safe for confidential documents.</p>
              </div>
            </div>
          </div>

          <div class="faq-item" itemprop="mainEntity" itemscope itemtype="https://schema.org/Question">
            <h3 itemprop="name" class="text-lg font-semibold mb-3">What's the difference between browser-based and cloud PDF tools?</h3>
            <div itemprop="acceptedAnswer" itemscope itemtype="https://schema.org/Answer">
              <div itemprop="text" class="text-[var(--text-dim)]">
                <p>Browser-based tools process files on your device (client-side) while cloud tools upload files to remote servers. Browser tools offer: complete privacy, offline capability, no upload time, HIPAA/GDPR compliance, and unlimited use. Cloud tools require internet, have file size limits, and pose privacy risks since your documents leave your control.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Layout>

<!-- JSON-LD Structured Data for SEO -->
<script type="application/ld+json" set:html={JSON.stringify({
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": title,
  "description": description,
  "url": "https://www.newlifesolutions.dev/hub/pdf-tools",
  "about": {
    "@type": "Thing",
    "name": "PDF Tools"
  },
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": tools.slice(0, 10).map((tool, index) => ({
      "@type": "ListItem",
      "position": index + 1,
      "name": tool.name,
      "description": tool.description,
      "url": `https://www.newlifesolutions.dev${tool.href}`
    }))
  }
})} />