---
/**
 * Guide: How to Generate Secure Password
 * Target query: "password generator", "random password", "secure password"
 */
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/ui/Navbar.astro';
import Footer from '../../components/ui/Footer.astro';

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "How secure are the generated passwords?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Extremely secure. We use Web Crypto API (crypto.getRandomValues) for cryptographically secure random generation."
      }
    },
    {
      "@type": "Question",
      "name": "Are my passwords saved anywhere?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "No. Passwords are generated entirely in your browser. We have no server, no database, and no way to see your passwords."
      }
    },
    {
      "@type": "Question",
      "name": "What makes a password strong?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Length (16+ characters) and variety (uppercase, lowercase, numbers, symbols). Our strength indicator helps you create uncrackable passwords."
      }
    }
  ]
};
---

<Layout
  title="Password Generator Free Online - Secure Random | New Life"
  description="Generate strong, secure random passwords instantly. Customize length (8-128), character types. Uses Web Crypto API. 100% private."
>
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

  <Navbar />

  <main class="pt-20 pb-16 px-6 min-h-screen">
    <article class="max-w-3xl mx-auto">

      <!-- Breadcrumb -->
      <nav class="text-sm text-[var(--text-muted)] mb-6">
        <a href="/" class="hover:text-[var(--accent)]">Home</a>
        <span class="mx-2">/</span>
        <a href="/guides" class="hover:text-[var(--accent)]">Guides</a>
        <span class="mx-2">/</span>
        <span>Generate Secure Password</span>
      </nav>

      <!-- Header -->
      <header class="mb-8">
        <span class="inline-block px-3 py-1 bg-emerald-500/20 text-emerald-400 rounded text-sm font-medium mb-4">
          Security Guide
        </span>
        <h1 class="text-4xl font-bold text-[var(--text)] mb-4">
          How to Generate Secure Password
        </h1>
        <p class="text-xl text-[var(--text-muted)]">
          Create strong, uncrackable passwords instantly. Customize length and character types for maximum security.
        </p>
      </header>

      <!-- Quick Answer -->
      <div class="bg-emerald-500/10 border border-emerald-500/30 rounded-xl p-6 mb-8">
        <h2 class="text-lg font-semibold text-emerald-400 mb-2">Quick Answer</h2>
        <p class="text-[var(--text)]">
          Use our <a href="/tools/password-generator" class="text-emerald-400 underline">free password generator</a>. Set length to 16+, include all character types, and copy the generated password. Use Web Crypto API for true randomness.
        </p>
      </div>

      <!-- What is Password Security -->
      <section class="mb-10">
        <h2 class="text-2xl font-bold text-[var(--text)] mb-4">What Makes a Password Secure?</h2>
        <p class="text-[var(--text-muted)] mb-4">
          <strong class="text-[var(--text)]">A strong password</strong> is long, random, and uses multiple character types. It should be unique for each account and impossible for humans or computers to guess.
        </p>
        <div class="bg-[var(--bg-secondary)] rounded-xl p-6 border border-[var(--border)]">
          <h3 class="font-semibold text-[var(--text)] mb-3">Characteristics of secure passwords:</h3>
          <ul class="space-y-2 text-[var(--text-muted)]">
            <li class="flex gap-3">
              <span class="text-emerald-400">1.</span>
              <span>Length of 16+ characters (longer is better)</span>
            </li>
            <li class="flex gap-3">
              <span class="text-emerald-400">2.</span>
              <span>Mix of uppercase, lowercase, numbers, and symbols</span>
            </li>
            <li class="flex gap-3">
              <span class="text-emerald-400">3.</span>
              <span>Truly random (not based on words or patterns)</span>
            </li>
            <li class="flex gap-3">
              <span class="text-emerald-400">4.</span>
              <span>Unique for each account (never reused)</span>
            </li>
          </ul>
        </div>
      </section>

      <!-- Steps -->
      <section class="mb-10">
        <h2 class="text-2xl font-bold text-[var(--text)] mb-6">Step-by-Step Instructions</h2>

        <div class="space-y-6">
          <div class="flex gap-4">
            <div class="flex-shrink-0 w-10 h-10 bg-[var(--accent)] text-[var(--bg)] rounded-full flex items-center justify-center font-bold">
              1
            </div>
            <div>
              <h3 class="text-lg font-semibold text-[var(--text)] mb-2">Set Password Options</h3>
              <p class="text-[var(--text-muted)]">
                Go to the <a href="/tools/password-generator" class="text-[var(--accent)]">password generator</a>. Set length to at least 16 characters. Enable all character types: uppercase, lowercase, numbers, and symbols.
              </p>
            </div>
          </div>

          <div class="flex gap-4">
            <div class="flex-shrink-0 w-10 h-10 bg-[var(--accent)] text-[var(--bg)] rounded-full flex items-center justify-center font-bold">
              2
            </div>
            <div>
              <h3 class="text-lg font-semibold text-[var(--text)] mb-2">Generate and Check Strength</h3>
              <p class="text-[var(--text-muted)]">
                Click "Generate Password". The tool creates a cryptographically secure random password using Web Crypto API. Check the strength indicator confirms it's uncrackable.
              </p>
            </div>
          </div>

          <div class="flex gap-4">
            <div class="flex-shrink-0 w-10 h-10 bg-[var(--accent)] text-[var(--bg)] rounded-full flex items-center justify-center font-bold">
              3
            </div>
            <div>
              <h3 class="text-lg font-semibold text-[var(--text)] mb-2">Copy and Store Securely</h3>
              <p class="text-[var(--text-muted)]">
                Click "Copy to Clipboard" and paste into your password manager or account signup. Never write passwords down or share them. Generate a new unique password for each account.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- CTA -->
      <div class="bg-[var(--bg-secondary)] rounded-xl p-8 text-center mb-10 border border-[var(--border)]">
        <h2 class="text-2xl font-bold text-[var(--text)] mb-4">Generate Password Now</h2>
        <p class="text-[var(--text-muted)] mb-6">
          Cryptographically secure, 100% private.
        </p>
        <a
          href="/tools/password-generator"
          class="inline-block px-8 py-4 bg-[var(--accent)] text-[var(--bg)] rounded-lg font-semibold hover:opacity-90 transition-opacity text-lg"
        >
          Open Password Generator
        </a>
      </div>

      <!-- Tips -->
      <section class="mb-10">
        <h2 class="text-2xl font-bold text-[var(--text)] mb-4">Tips for Best Results</h2>
        <div class="grid md:grid-cols-2 gap-4">
          <div class="bg-green-500/10 border border-green-500/30 rounded-lg p-4">
            <h3 class="font-semibold text-green-400 mb-2">Do</h3>
            <ul class="text-sm text-[var(--text-muted)] space-y-1">
              <li>Use 16+ characters for important accounts</li>
              <li>Enable all character types (letters, numbers, symbols)</li>
              <li>Use a password manager to store them</li>
              <li>Generate unique password for each site</li>
            </ul>
          </div>
          <div class="bg-red-500/10 border border-red-500/30 rounded-lg p-4">
            <h3 class="font-semibold text-red-400 mb-2">Avoid</h3>
            <ul class="text-sm text-[var(--text-muted)] space-y-1">
              <li>Reusing passwords across sites</li>
              <li>Using personal info or dictionary words</li>
              <li>Writing passwords on paper or notes apps</li>
              <li>Sharing passwords via email or chat</li>
            </ul>
          </div>
        </div>
      </section>

      <!-- FAQ -->
      <section class="mb-10">
        <h2 class="text-2xl font-bold text-[var(--text)] mb-6">Frequently Asked Questions</h2>
        <div class="space-y-4">
          <details class="bg-[var(--bg-secondary)] rounded-lg border border-[var(--border)]">
            <summary class="px-6 py-4 cursor-pointer font-medium text-[var(--text)]">
              How secure are the generated passwords?
            </summary>
            <p class="px-6 pb-4 text-[var(--text-muted)]">
              Extremely secure. We use Web Crypto API (crypto.getRandomValues) for cryptographically secure random generation, not pseudo-random algorithms.
            </p>
          </details>
          <details class="bg-[var(--bg-secondary)] rounded-lg border border-[var(--border)]">
            <summary class="px-6 py-4 cursor-pointer font-medium text-[var(--text)]">
              Are my passwords saved anywhere?
            </summary>
            <p class="px-6 pb-4 text-[var(--text-muted)]">
              No. Passwords are generated entirely in your browser. We have no server, no database, and no way to see or store your passwords. Once you close the tab, they're gone.
            </p>
          </details>
          <details class="bg-[var(--bg-secondary)] rounded-lg border border-[var(--border)]">
            <summary class="px-6 py-4 cursor-pointer font-medium text-[var(--text)]">
              What makes a password strong?
            </summary>
            <p class="px-6 pb-4 text-[var(--text-muted)]">
              Length is most important (16+ characters). Use variety (uppercase, lowercase, numbers, symbols). Avoid patterns, words, and personal information. Our strength indicator helps verify.
            </p>
          </details>
          <details class="bg-[var(--bg-secondary)] rounded-lg border border-[var(--border)]">
            <summary class="px-6 py-4 cursor-pointer font-medium text-[var(--text)]">
              How long should my password be?
            </summary>
            <p class="px-6 pb-4 text-[var(--text-muted)]">
              Minimum 16 characters for important accounts. 20+ characters provides even better security against brute force attacks. Every additional character exponentially increases security.
            </p>
          </details>
        </div>
      </section>

      <!-- Related -->
      <section>
        <h2 class="text-2xl font-bold text-[var(--text)] mb-4">Related Tools</h2>
        <div class="flex flex-wrap gap-3">
          <a href="/tools/qr-generator" class="px-4 py-2 bg-[var(--bg-secondary)] rounded-lg text-[var(--text-muted)] hover:text-[var(--accent)] border border-[var(--border)]">
            QR Generator
          </a>
          <a href="/tools/hash-generator" class="px-4 py-2 bg-[var(--bg-secondary)] rounded-lg text-[var(--text-muted)] hover:text-[var(--accent)] border border-[var(--border)]">
            Hash Generator
          </a>
          <a href="/tools/pdf-redactor" class="px-4 py-2 bg-[var(--bg-secondary)] rounded-lg text-[var(--text-muted)] hover:text-[var(--accent)] border border-[var(--border)]">
            PDF Redactor
          </a>
        </div>
      </section>

    </article>
  </main>

  <Footer />
</Layout>
